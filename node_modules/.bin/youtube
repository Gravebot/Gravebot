#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../youtube-node/bin/youtube" "$@"
  ret=$?
else 
  node  "$basedir/../youtube-node/bin/youtube" "$@"
  ret=$?
fi
exit $ret
